const React = require('react');
const ReactDom = require('react-dom');

const WordRelay = require('./WordRelay'); // 쪼갠 파일 WordRelay.jsx을 불러오기

ReactDom.render(<WordRelay />, document.querySelector( selectors, '#root'));