/*
    동기 : 위에서 부터 순차적으로 실행
    비동기 : click, submit 등 동작 이후에 실행
*/

폼.addEventListener('submit', function 콜백함수 (e) { //e 는 매개변수
    e.preventDefault(); // 폼은 submit을 하면 새로고침이 기본동작, 이 *기본동작을 막기 = 새로고침이 되어버리면 이벤트 저장된 값이 ㅂ2ㅂ2 
    if (  ) {
       
    } else {

    };
});